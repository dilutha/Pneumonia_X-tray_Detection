# ── Web Framework ──────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1      # ASGI server (runs FastAPI)
python-multipart==0.0.12       # Required for file upload parsing

# ── Settings & Validation ──────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1       # Environment variable management

# ── ML & Image Processing ──────────────────────────────────────
tensorflow==2.17.0             # Your model runtime
numpy==1.26.4
Pillow==11.0.0                 # PIL — image loading, resizing
opencv-python-headless==4.10.0.84  # OpenCV without GUI (for Grad-CAM overlay)
scipy==1.14.1                  # For heatmap post-processing

# ── Database ───────────────────────────────────────────────────
supabase==2.10.0               # Supabase Python client
asyncpg==0.30.0                # Async PostgreSQL driver
sqlalchemy==2.0.36             # ORM (for model definitions)

# ── HTTP & Utilities ───────────────────────────────────────────
httpx==0.27.0                  # Async HTTP client
python-jose[cryptography]==3.3.0   # JWT (for future auth)
passlib[bcrypt]==1.7.4         # Password hashing (for future auth)

# ── Production ─────────────────────────────────────────────────
gunicorn==23.0.0               # Production WSGI server